# Lane Oil Pattern 3D

볼링 레인 오일 패턴 시트(Kegel / FLEX)의 수치 데이터를 그대로 입력받아 웹 브라우저에서
**3D 레인 위에 렌더링·분석**하고, 볼러/볼 스펙으로 **실제 샷을 시뮬레이션**하는 React 앱입니다.
패턴표의 그래프를 재현하는 **패턴표 모드**와 PBA 중계 화면 같은 **실제 레인 모드**를 모두
지원하며, 스키드 → 훅 → 롤로 이어지는 볼의 궤적과 회전·트랙 플레어를 3D로 재생합니다.

밝고 모던한 인포그래픽 스타일 UI로, 라이트/다크 테마와 모바일 레이아웃을 지원합니다.

> **Live demo**: https://fearnot502.github.io/3DBowlingLane/

## 기술 스택

- **React 18 + JavaScript** (Vite 5)
- **Three.js** + `@react-three/fiber` + `@react-three/drei`
- **Tailwind CSS**
- `pdfjs-dist` (패턴 시트 PDF 텍스트 추출 / 미리보기 렌더링)
- 상태·저장은 전부 클라이언트(`localStorage`) — 백엔드 없음

## 실행 & 배포

```bash
npm install
npm run dev      # 개발 서버 (기본 http://localhost:5173)
npm run build    # 프로덕션 번들 → dist/
npm run preview  # 빌드 결과 로컬 확인
```

`main` 브랜치에 푸시하면 GitHub Actions(`.github/workflows/deploy.yml`)가 자동으로
빌드해서 **GitHub Pages**에 배포합니다. 저장소 *Settings → Pages → Source*를
**GitHub Actions**로 설정해 두면 됩니다. (`vite.config.js`의 `base: './'` 덕분에
프로젝트 서브패스에서도 그대로 동작합니다. 백엔드 없이 완전한 정적 사이트입니다.)

## 조작

| 동작 | 방법 |
|------|------|
| 회전 / 줌 / 이동 | 드래그 / 휠 / 우클릭 드래그 |
| 카메라 이동 | W·A·S·D, 상승 E / 하강 Shift |
| 재생 · 일시정지 | 스페이스바(플레이 탭) 또는 재생 버튼 |
| 시점 프리셋 | 보기 탭 → 카메라 시점 (기본·볼러 시점·측면·탑다운) |

## 주요 기능

### 3D 레인
- 실제 규격(60ft × 39보드 ≈ 40.6") 기반 레인 — 가이드 도트(6ft), 타깃 화살표(12–16ft),
  레인지 파인더(34/40ft), 거터, 우드 핀덱과 10핀
- **레인 폭 배율** 슬라이더: 기본 5×(패턴표 그래프와 같은 비율), 1× = 실제 비율
- `OrbitControls`로 회전·확대·이동, 5ft 간격 거리 라벨, 패턴 끝(끝 Nft) 마커
- **카메라 시점 프리셋**: 기본 / 볼러 시점(어프로치 눈높이) / 측면 / 탑다운을 원터치로 이동한 뒤 자유 회전

### 오일 시각화 — 2가지 모드
- **패턴표 모드**: 인쇄된 시트의 3색 범례를 그대로 재현 — 색은 레이어 구성만 표시
  (Forward=시안, Reverse=블루, 겹침=네이비), 오일 **양은 높이**(displacement)로 표현
- **실제 레인 모드**: PBA 중계처럼 오일이 나무 위의 반투명 광택 글레이즈로 보이는 모드 —
  밀도를 부드럽게 압축(Reinhard)해 두꺼운 헤드 구간도 자연스럽게 깊어지고, 보드가 항상 비쳐 보임
- Forward / Reverse 레이어 토글, 불투명도·두께 슬라이더, 패턴 상하 반전 옵션

### 샷 시뮬레이션 & 3D 볼 (플레이 탭)
볼러/볼 스펙을 입력하면 지금 올려둔 오일 패턴 위로 실제 샷을 물리 시뮬레이션합니다.

- **볼러 스펙**: 오른손/왼손, 프리셋(스트로커·트위너·크랭커), 볼 스피드(km/h), 회전수(Rev Rate)
- **릴리스 축**: 축 회전(Axis Rotation, 사이드 로테이션 = 훅 길이) · 축 틸트(Axis Tilt, 스필러 = 플레어 감소·조기 롤).
  기본 55°/15°에서는 기존 캘리브레이션과 동일하고, 값을 바꾸면 훅량과 플레어가 변합니다
- **볼 스펙**: RG(스키드 길이) · Differential(전체 훅/플레어) · PSA(브레이크포인트 각짐)
- **내 라인**: 스탠스(레이다운) 보드와 타겟(에로우 15ft) 보드를 조절 → 엔트리 보드·진입각·
  브레이크포인트·판정(포켓/두꺼움/얇음/거터)을 실시간 표시. 슬라이더는 **실제 장수 읽는 순서**
  (오른손 40→1)로 배치되고 손에 따라 자동 미러링됩니다
- **라인 추천**: 현재 패턴에서 관용도 높은 **윗장(바깥)·아랫장(안쪽)** 라인을 탐색해
  근거(진입각, 브레이크포인트, Rule of 31 예상, 미스 관용도, 경로 평균 오일)와 함께 제시
- **3D 재생**: 궤적을 스키드(시안)·훅(주황)·롤(빨강) 위상별로 색칠해 그리고, 실제 규격의 볼이
  라인을 따라 굴러가며 재생 — 재생/일시정지·속도 조절, 반복 재생, **타임라인 스크러버**로 임의 지점 확인
- **궤적 분석 차트**: 거리별 스피드·슬립(스키드→롤)·브레이크포인트를 한눈에

### 볼 인스펙터 창
- 화면 우상단에 공만 따로 확대해 띄우는 별도 창(독립 캔버스). 메인 재생과 **공유 클럭으로 동기화**되어
  볼의 회전과 오일 트랙 플레어가 생기는 과정을 그대로 보여줍니다. 드래그로 돌려볼 수 있고, X로 닫기/보기 탭에서 다시 켜기

### 오일 트랜지션 (브레이크다운 · 캐리다운)
- "진행 샷 수"를 올리면 **플레이한 라인(볼 트랙)을 따라** 헤드·미들 오일이 마르고(브레이크다운),
  패턴 끝 뒤로 얇은 필름이 밀려납니다(캐리다운)
- **실제 레인 모드에서 눈에 보이며**, 궤적·추천 라인에도 반영됩니다 (패턴표 모드는 갓 깐 프레시 패턴 유지)
- 트랙은 처음 적용한 라인으로 **고정(스냅샷)** — 스팟을 옮기거나 라인을 적용해도 오일 표시는 그대로이고
  볼 반응만 바뀝니다. **초기화** 버튼으로 프레시 상태 복귀

### 내 셋업 (아스날)
- 현재 볼러·볼 스펙 + 라인을 통째로 이름 붙여 저장/불러오기/삭제 (`localStorage`).
  어떤 패턴에서도 내 장비 세팅을 바로 불러와 비교할 수 있습니다

### 볼 물리 모델 (`lib/ballMotion.js`)
0.25ft씩 레인을 내려가며 매 스텝의 **로컬 오일 밀도로 마찰**을 정하는 격자 기반 시뮬레이션.
마찰이 실제 레인처럼 세 가지 일을 합니다.

1. 볼을 **스핀업**(slip 1 → 0): 스키드 → 훅 → 롤 전환
2. 슬립이 남아 있는 동안 볼을 **옆으로 밀어냄**(훅)
3. 전진 **속도를 깎음**

볼 스펙은 볼러가 쓰는 감각 그대로 매핑됩니다 — **RG**↑ 스핀업 저항(늦은 브레이크),
**Diff**는 플레어 포텐셜(총 훅량 스케일), **PSA**는 브레이크포인트에서의 급격함(부드러운 아크 ↔ 날카로운 플립),
**축 회전**↑ 훅 길이 증가, **축 틸트**↑ 플레어·훅 감소.
좌표는 **볼러 보드**(사용 손 쪽 거터에서 안쪽으로 센 번호)로 계산해 한 코드 경로로 좌우손을 모두 처리합니다.

### 회전 & 트랙 플레어 (`components/BallPath.jsx`)
현실의 회전 역학을 최대한 반영했습니다.

- **렙업(rev-up)**: 스키드 구간은 릴리스 회전수로, 롤 구간은 노면 속도(`v/r`)로 도는 블렌딩
- **축 이행**: 릴리스 때 진행방향으로 기운 스핀축(사이드 로테이션 = 훅)에서 슬립이 줄며 순수 엔드오버엔드(롤)로 이행
- **트랙 플레어(보타이)**: 오일 링을 **회전수 기준**으로 남겨(렙수↑ = 링 촘촘), 스핀축이 한 평면 안에서
  이행하도록 해 모든 링이 두 매듭점을 통과하는 실제 오일트랙 **보타이** 형태를 형성. 이행량은 Diff에 비례,
  축 틸트에 반비례하며 슬립에 비례해 누적되므로, 롤에 들면(slip→0) 이행이 멈춰 링들이 매듭으로 수렴
- 링 데이터는 샷마다 **결정론적으로 미리 계산**해, 메인 볼·인스펙터·스크럽이 정확히 같은 플레어를 공유

### 데이터 입력
- **표 붙여넣기**: `START STOP LOADS SPEED BUFFER [TANK] CROSSED START END FEET T.OIL`
  형식의 행을 그대로 붙여넣으면 파싱 (TANK 열·행 번호 유무 자동 인식)
- **PDF 가져오기**: 텍스트가 살아 있는 시트는 메타데이터·표를 자동 추출, 이미지형 시트는 1페이지를 미리보기로 렌더링
- **AI 가져오기**: 이미지형 시트를 AI 챗에 넣어 변환한 텍스트를 붙여넣는 플로우 —
  결과는 `localStorage`에 저장되어 새로고침 후에도 "내 패턴" 목록에 유지
- 내장 샘플: **2026 JINSEUNG A TYPE** (45ft, 29.5 mL)

### 분석
- 보드별 오일 분포 스택 차트(시트 하단 차트와 동일 형식), 피크 보드, 실측 패턴 길이
- 거리별 오일 단면(슬라이스) — 3D 레인에 위치선 표시
- 시트의 Track Zone Ratio 표 표시 (머신 고유 알고리즘 값이므로 재계산하지 않음)

### UI / 레이아웃
- **인포그래픽 스타일**: 이모지 없이 SVG 스트로크 아이콘만 사용, 카드 스택 + 블루 액센트, mono 숫자 중심의 스탯 타일
- **라이트 / 다크 테마 토글**: 3D 배경·바닥까지 테마 연동, 선택은 `localStorage`에 유지
- **데스크톱**: 좌측 사이드바를 **패턴 / 보기 / 플레이 / 분석** 4개 탭으로 분리
- **모바일**: 3D가 항상 배경에 보이고, 아래에서 끌어올리는 **바텀 시트**(접힘/절반/전체 3단 스냅)에 컨트롤이 담김
- **플로팅 툴바**: 오일 모드·레이어·라벨·핀·폭 배율 등 자주 쓰는 컨트롤을 3D 캔버스 위 컴팩트 바로 노출

## 데이터 해석 규칙

### 보드 표기 (중요)
Kegel 표기는 **각 거터에서 안쪽으로 센 번호**입니다:

| 표기 | 의미 | 절대 보드 |
|------|------|-----------|
| `4L` | 왼쪽 거터에서 4번째 | 4 |
| `4R` | 오른쪽 거터에서 4번째 | 36 (= 40 − 4) |
| `20` | 센터 보드 | 20 |

따라서 `4L-4R`은 보드 4~36의 **넓은** 스팬, `14L-15R`은 보드 14~25의 **좁은** 스팬입니다.
시트의 `CROSSED = LOADS × 지나간 보드 수`로 검증됩니다.

> 샷 시뮬레이션의 **볼러 보드**는 이와 별개로, 사용 손 쪽 거터에서 안쪽으로 센 번호입니다
> (오른손 1-3 포켓 / 왼손 1-2 포켓 = 17.5보드). 플레이 탭 슬라이더는 실제 장수 읽는 방향(오른손 40→1)으로 표시됩니다.

### 오일 밀도 모델
각 패스는 보드 범위 × 거리 범위가 만드는 사각형에 오일을 도포한다고 보고,
해상도와 무관한 밀도(µl per board·foot)로 누적합니다:

```
density = T.OIL ÷ (보드 수 × 피트 길이)
```

- `T.OIL`이 없으면 `LOADS × 50µl × 보드 수`로 추정 (LOADS=0 이동 행은 오일 없음)
- 버퍼 브러시의 전폭 얇은 필름(Forward: 파울라인→버프아웃, Reverse: 브러시 드롭→파울라인)과
  마지막 로드를 파울라인까지 끌고 가는 드래그를 합성해 인쇄 그래프와 같은 형태를 재현
- 좌우로 가우시안 블러(≈2.2보드)를 적용해 브러시가 옆으로 번지는 어깨 그라데이션 표현
- 합산 오일 총량은 시트의 Forward/Reverse/Volume Total과 일치 (JINSEUNG A: 19.05 / 10.45 / 29.5 mL)

### 오일 트랜지션 모델
`oilModel.js`의 `ageOilGrid`가 진행 샷 수를 받아 **트랙을 따라** 패턴을 노화시킵니다 —
헤드/미들에 브레이크다운(오일 제거), 패턴 끝 뒤에 캐리다운(얇은 필름 도포). 트랙은 프레시 그리드에서 뽑은
샷 경로의 스냅샷을 쓰며(순환 의존 차단), 물리(sim·추천)에는 항상, 디스플레이에는 실제 레인 모드에서만 반영됩니다.
휴리스틱 모델이라 계수는 튜닝 대상입니다.

## 구조

```
src/
  lib/
    laneConstants.js  레인 규격·그리드 해상도 상수
    boardUtils.js     L/R 보드 표기 ↔ 절대 보드(1~39) 변환
    parsePattern.js   시트 표 텍스트 파서
    oilModel.js       오일 밀도 그리드 계산 (버퍼 필름·드래그, 트랜지션 ageOilGrid)
    ballMotion.js     샷 물리 시뮬레이션·라인 추천 (스키드/훅/롤, RG·Diff·PSA·축)
    colorScale.js     밀도/레이어 → 색상 램프
    oilTexture.js     밀도 그리드 → 색상·변위 DataTexture (sheet/realistic)
    laneTexture.js    우드 레인·핀덱 캔버스 텍스처
    pdfImport.js      PDF 텍스트 추출·미리보기 렌더링
    aiImport.js       AI 변환 텍스트 파서 (프롬프트 템플릿 포함)
    storage.js        내 패턴·내 셋업(아스날) localStorage 저장
    analysis.js       보드별 분포·파생 통계
  components/
    Scene.jsx             Canvas·조명·OrbitControls·카메라 프리셋 릭 (테마 연동 배경)
    Lane.jsx              레인·거터·핀·오일 표면·라벨·마커
    BallPath.jsx          3D 샷 궤적·구르는 볼(FlareBall, 회전·렙업)·트랙 플레어 링·타임라인 스크럽
    BallInspector.jsx     우상단 볼 인스펙터 창 (독립 캔버스, 공유 재생 클럭)
    ShotChart.jsx         스피드·슬립 궤적 차트
    ControlPanel.jsx      탭 라우팅 + 탭바 (패턴/보기/플레이/분석)
    tabs/
      PatternTab.jsx      패턴 불러오기(AI·PDF·샘플)·시트 메타데이터
      ViewTab.jsx         카메라 시점·오일 모드·레이어·시각화 슬라이더
      PlayTab.jsx         볼러/볼 스펙·아스날·내 라인 시뮬·트랜지션·추천·재생 컨트롤
      AnalysisTab.jsx     분포 차트·단면·패스 표·직접 편집·Track Zone
    Toolbar.jsx           3D 캔버스 위 컴팩트 플로팅 툴바
    BottomSheet.jsx       모바일 드래그 바텀 시트 (3단 스냅)
    ui.jsx                공용 UI 프리미티브 (버튼·토글·슬라이더·카드…)
    icons.jsx             SVG 스트로크 아이콘 세트 + 로고
    Legend.jsx            색상 범례
    BoardProfileChart.jsx 보드별 분포 차트
  App.jsx                 레이아웃(데스크톱 사이드바 / 모바일 시트)·상태·테마
  data/samplePatterns.js  내장 샘플 패턴
```

## 참고

볼 물리와 오일 트랜지션은 **휴리스틱 모델**입니다. 실제 레인 표면·볼 커버스톡 상태·환경에 따라 결과가
달라질 수 있으며, 정성적인 경향을 읽는 도구로 사용하세요.
